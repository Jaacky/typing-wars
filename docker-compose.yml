version: '3'
services:
  typingwars:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: typingwars
    ports:
      - "8080:8080"
  
  nginx:
    build:
      context: ./proxy
      dockerfile: Dockerfile
    container_name: nginx
    ports:
      - "80:80"
      - "443:443"
